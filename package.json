{
  "name": "liigaporssi",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "db:local": "wrangler d1 migrations apply liigaporssi --local",
    "db:remote": "wrangler d1 migrations apply liigaporssi --remote",
    "image": "node scripts/optimize-image.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260907.1",
    "sharp": "^0.35.4",
    "typescript": "^5.6.0",
    "vitest": "^5.0.0",
    "wrangler": "^4.0.0"
  }
}
